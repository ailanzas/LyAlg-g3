#include <iostream>
#include <math.h>
#include <string.h>

using namespace std;

int main(int argc, char const *argv[])
{
/* iostream */
    cout << "\t Hola \n esto es un mensaje.. " << endl;

    /*math*/
    int num = 81;
    double raiz = sqrt(num);
    cout << "La raiz cuadrada de " << num << " es " << raiz << endl;
    int x = 3;
    long int potencia = pow(x, 60);
    cout << potencia<<endl;
    return 0;
}
